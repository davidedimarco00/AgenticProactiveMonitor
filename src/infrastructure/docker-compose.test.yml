services:
  agentic-backend:
    environment:
      ENABLE_TEST_ANOMALY_INJECTION: "1"
      ENABLE_OPENSEARCH_ANOMALY_WATCHER: "0"
      AGENT_REACT_MAX_STEPS: ${AGENT_REACT_MAX_STEPS:-6}
      AGENT_REACT_TOOL_TIMEOUT_SECONDS: ${AGENT_REACT_TOOL_TIMEOUT_SECONDS:-30}
