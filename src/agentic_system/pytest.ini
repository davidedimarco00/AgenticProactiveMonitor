[pytest]
testpaths = tests
markers =
    integration: integration tests against the running Dockerized agentic backend
    fault: opt-in destructive fault-injection tests that temporarily alter XMPP sessions
addopts = -ra
